import { ModalWrapper } from "@/features/Modals/ModalWrapper";

export const NewElementModal = () => {
    return (
        <ModalWrapper>
            <div>some</div>
        </ModalWrapper>
    );
};
