# api preview component

## front-end

- [x] make a new section in the page for the api, the new section should be toggleable
- [x] fix the design for the api section

### interactive api component

- [x] create a base UI for the api preview with following components
    - [x] folder like collapsable component with circle and curves to showing the branching out
          contents
    - [x] on the right show the access key value by which user can get the corresponding value
    - [x] a line which connects the access key value and the object name
    - [x] collapsable indicator
    - [x] key and value
- [x] make the component recursive so that it will form the tree like structure based on the api
      (json format) - [ ] the access key should be dynamic
- [ ] add hover effect by highlight the entire tree path
- [ ] selection effect by highlight the entire tree path and circle component

### interactive url component

### api response view after the url params

## backend-end

<!-- needs to updat the todo -->

#### notes

- the entries should be collapsable
- things should have different colors for better readbility
- there should be line numbers
- add a feidl for the url with interactive params
- the api should reflect the data based on the url params

### misc

- [x] make the side bar component toggleable based on the selected type
