# Table block

## backend

### api

- columns
- [x] base route for the columns
- [x] add a `GET` route to read all columns based on the `table_id`
- [x] add a `POST` route to create a new column based on the `table_id`
- [ ] add a `DELETE` route to delete all columns based on the `table_id`
- [ ] add a `PATCH` route to update column based on the `table_id` and `column_id`

- rows
- [ ] create operation route for rows
- [ ] read all operation route for rows
- [ ] read by id operation route for rows
- [ ] delete operation route for rows
- [ ] update operation route for rows
